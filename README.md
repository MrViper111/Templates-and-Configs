# Templates-and-Configs
A bunch of templates and configs that I use for several different applications / tools.

`.tmux.conf` - tmux config

`.zshrc` - zshrc shell config

`config.ghostty` - config for ghostty terminal emulator

`init.lua` - neovim config

`starship.toml` - starship (more terminal tools) config
